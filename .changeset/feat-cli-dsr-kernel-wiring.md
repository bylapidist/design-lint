---
'@lapidist/design-lint': minor
---

feat(cli): wire CLI to DSR kernel — auto-detect running kernel socket and pass dsr option to createNodeEnvironment; add --kernel/--no-kernel flags and --kernel-socket-path option so the v8 architecture is active by default when the kernel is running

/**
 * @packageDocumentation
 *
 * JSON formatter for machine-readable lint results.
 *
 * Serializes {@link LintResult} arrays into a human-friendly JSON string.
 */
export { jsonFormatter } from './json-formatter.js';
